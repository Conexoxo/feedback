<%= form_for([@course, @comment]) do |form| %>
  <div class="field bg-white mt-3">
    <%= form.rich_text_area :comment %>
  </div>

  <div class="actions">
    <%= form.submit 'Guardar' , class: 'btn btn-primary mt-2' %>
  </div>
<% end %>