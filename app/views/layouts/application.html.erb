<!DOCTYPE html>
<html>
  <head>
    <title>Proyecto</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>


    <ul class="nav justify-content-end">
      <%- if teacher_signed_in? %>
        <li class="nav-item">
          <%= link_to 'Cerrar sesión', destroy_teacher_session_path, method: :delete%>
          <li class="nav-item">
              <%= link_to 'Conoce tu evalucación docente'%>
          <li class="nav-item">
              <%= link_to 'Blog de comentarios', images_path %>
        </li>
        <%else%>
          <%- if student_signed_in? %>
             <li class="nav-item">
               <%= link_to 'Cerrar sesión', destroy_student_session_path, method: :delete%>
              <li class="nav-item">
               <%= link_to 'Blog de comentarios'%>
          </li>
            <%else%>
              <li class="nav-item">
                <%= link_to 'Iniciar sesión teacher', new_teacher_session_path %>
              <li class="nav-item">
                <%= link_to 'Registrar teacher', new_teacher_registration_path %>
              <li class="nav-item">
                <%= link_to 'Iniciar sesión Alumno', new_student_session_path %>
              <li class="nav-item">
                <%= link_to 'Registrar Alumno', new_student_registration_path %>
            </li>

            
          <% end %>       
   <% end %>   

    <p class="notice"><%= notice %></p>
       <p class="alert"><%= alert %></p>
    <%= yield %>
  </body>
</html>
