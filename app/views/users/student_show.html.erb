<div class="container mt-3">
	<h1 class="center" style="color:white !important; margin-top: 2rem;">Mis cursos</h1>
	<h6 class="center" style="color:white !important;">Total: <%= @courses_count %></h6>

	<table class="table table-dark">
		<thead>
			<tr>
				<th scope="col">NRC</th>
				<th scope="col">Nombre</th>
			</tr>
		</thead>
		<tbody>
			<% @teacher_courses.each do |course| %>
				<tr>
					<td><%= course.nrc %></td>
					<td><%= course.name %></td>
				</tr>
			<% end %>
		</tbody>
	</table>
</div>